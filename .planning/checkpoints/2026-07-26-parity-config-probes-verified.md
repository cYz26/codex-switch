---
checkpoint_id: 2026-07-26-parity-config-probes-verified
created_at: 2026-07-26T13:10:28+08:00
boundary: verification_passed
project_mode: brownfield
change_id: internal-official-feature-parity
compact_recommended: true
compact_status: pending
next_stage: Execute task 4.1 RED schema-v3 journal tests
---

# Checkpoint: Internal Official Feature Parity Config and Probes Verified

Date: 2026-07-26 13:10:28 +0800
Goal: `internal-official-feature-parity`
Change: `internal-official-feature-parity`
Boundary: `verification_passed`
Status: `CHECKPOINT_AND_CONTINUE`

## Current goal

Complete the approved `internal-official-feature-parity` Full OpenSpec while
preserving the five allowed internal identity differences and every explicit
live, dependency, Git, release, archive, cleanup, and migration gate.

## Resume State

- Repository: `/Users/cY/dev/codex-switch`
- Branch: `main`
- `HEAD` and `origin/main`: `0a9400d`
- Existing dirty-worktree changes: preserved
- Pre-checkpoint worktree status SHA-256:
  `0fd7a08a8200e55a15bef58fe1ae2dc50916e084c09940b06b4e0b1261b9c39d`
- OpenSpec schema: `spec-driven`
- OpenSpec progress: 29/79
- Completed slice: Config/probes, tasks 3.1-3.9
- Next dependency-ready item: task 4.1 RED
- Task 8.1 remains the explicit live Human Gate

## Completed work

The Config/probes slice now provides:

- an internal-only overlay/v2 config projection;
- exact, ambiguity-safe removal of scalar `agents.max_threads`;
- staged managed-home derivation that cannot recover stale runtime values;
- bounded candidate-only schema/core/typed-explorer-v2 probes;
- process-group timeout and output-bound enforcement;
- stable sanitized evidence digests; and
- a synthetic retained provider fixture proving transient credentials and
  configs are neither retained nor copied.

Task 3.9 changed no production source. It reran the complete supported slice
matrix and closed the slice at 29/79.

## Durable context written

- `openspec/changes/internal-official-feature-parity/tasks.md`
- `TASK_LEDGER.md`
- `.planning/STATE.md`
- `.planning/devflow/verification/internal-official-feature-parity.md`
- `.planning/checkpoints/2026-07-26-parity-config-probes-verified.md`

## Validation performed

```text
Complete parity
Python 3.12.13: 82/82
system Python 3.9.6: 82/82
```

```text
Config Document
Python 3.12.13: 29/29
system Python 3.9.6 direct baseline: 3 failures, 22 errors
```

The system-Python Config Document result is the established unsupported
Python-3.11+-`tomllib` boundary. The bounded-parser changed paths pass on
system Python through the parity and focused projection tests.

```text
Focused home-sync projection: 2/2 on both runtimes
Verifier Python 3.12.13: 22/22
Verifier supported system-Python route: 22/22
Native system-Python sanitizer focus: 2/2
Strict active OpenSpec: passed
Dual-runtime AST: 56/56 scripts
Dual-runtime production imports: passed
git diff --check: passed
generated bytecode cleanup: passed
```

The supported system-Python verifier route sets
`CODEX_SWITCH_PYTHON=/opt/homebrew/bin/python3.12` for generated managed
wrappers. Legacy DevFlow skill-layout recommendations remain nonblocking and
untouched.

## Key decisions

- Task 4.1 is tests-only RED. Do not implement schema-v3 promotion behavior
  before the exact required and optional target-set failures are captured.
- Manifest, launcher, capability receipt, parity receipt, overlay, and profile
  config are required targets.
- Shared config and active runtime config are optional only under the exact
  conditions specified by the approved design.
- Existing schema-v1/v2 recovery remains unchanged until its later dedicated
  compatibility task.

## Open questions

None. The next RED contract and write set are explicit.

## Risks

- No runtime bundle or caller consumes parity artifacts yet.
- Transaction task 4 can expose target-set or recovery assumptions shared with
  existing Runtime Binding and transaction code.
- Live provider, App, profile, update, release, Git, archive, and cleanup
  effects remain outside the current authorization.

## Next action

Execute task 4.1 by RED in `scripts/test_codex_transaction.py`. Require the
exact schema-v3 text-artifact target set and optional-target rules, then run the
focused failures on Python 3.12.13 and system Python 3.9.6. Do not add
production behavior until the RED evidence is recorded.

## Compact instruction

Compact is recommended at this verified slice boundary, but continuation is
safe from this checkpoint. Continue directly to task 4.1 without requesting
routine confirmation.
